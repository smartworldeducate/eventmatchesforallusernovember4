import { View, Text } from 'react-native'
import React from 'react'

const TestComponent = () => {
  return (
    <View style={{flex:'1'}}>
      <Text style={{color:'red'}}>TestComponent</Text>
    </View>
  )
}

export default TestComponent